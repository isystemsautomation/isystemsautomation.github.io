{
  "name": "isystemsautomation-site",
  "private": true,
  "scripts": {
    "build": "node tools/generate-pages.mjs && eleventy",
    "verify": "node tools/verify-content.mjs",
    "optimize-images": "node tools/optimize-images.mjs",
    "serve": "npm run build && eleventy --serve"
  },
  "devDependencies": {
    "@11ty/eleventy": "^3.0.0",
    "cheerio": "^1.2.0"
  }
}
